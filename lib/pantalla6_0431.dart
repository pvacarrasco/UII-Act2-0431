import 'package:flutter/material.dart';

class Pantalla6_0431 extends StatelessWidget {
  const Pantalla6_0431({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Pantalla6 Armenta0431"),
        backgroundColor: Color(0xff70a29c),
      ),
      backgroundColor: Color(0xffa7b4ec),
      body: Container(
        color: Color(0xff628bd7),
        child: Text(
          'I am a text',
          style: TextStyle(fontSize: 38, color: Color(0xffffffff)),
        ),
      ),
    );
  } // fin widget
} //fin pantalla 5
